interface:
  display_name: "Litellm"
  short_description: "Repo-specific patterns and workflows for litellm"
  default_prompt: "Use the litellm repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true